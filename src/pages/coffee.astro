---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="레퍼런스커피 - RefX" description="기술 스타트업이 만드는 프리미엄 커피 브랜드, 레퍼런스커피">
  <Header />
  <main>
    <!-- Hero: Full-bleed Coffee Dark -->
    <section class="bg-coffee-900 text-white py-28 lg:py-40 relative overflow-hidden">
      <div class="absolute inset-0 bg-gradient-to-br from-coffee-900 via-coffee-800 to-coffee-700 opacity-90"></div>
      <div class="relative max-w-8xl mx-auto px-6 lg:px-8">
        <div class="max-w-2xl">
          <span class="font-display text-label text-coffee-300 tracking-widest uppercase block mb-4">Reference Coffee</span>
          <h1 class="font-display font-black text-5xl md:text-6xl leading-tight mb-6">
            ☕ 코드처럼<br>정밀하게<br>로스팅된 <span class="text-coffee-300">커피</span>
          </h1>
          <p class="text-coffee-100 text-lg leading-relaxed mb-10">
            엄선된 원두, 데이터 기반 로스팅 프로파일.<br>기술 스타트업이 만드는 프리미엄 커피.
          </p>
          <div class="flex flex-wrap gap-4">
            <a href="https://smartstore.naver.com/refcoffee" target="_blank" rel="noopener noreferrer"
              class="px-6 py-3 bg-coffee-400 text-white font-display font-bold rounded-btn hover:bg-coffee-600 transition-colors">
              스마트스토어에서 구매 →
            </a>
            <a href="https://instagram.com/refx.coffee" target="_blank" rel="noopener noreferrer"
              class="px-6 py-3 border border-coffee-400 text-coffee-300 font-display font-bold rounded-btn hover:bg-coffee-800 transition-colors">
              인스타그램 팔로우
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Brand Story -->
    <section class="py-24 bg-white dark:bg-slate-900">
      <div class="max-w-8xl mx-auto px-6 lg:px-8">
        <div class="grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <span class="font-display text-label text-terra tracking-widest uppercase block mb-3">Brand Story</span>
            <h2 class="font-display font-bold text-4xl text-gray-900 dark:text-white mb-6">
              기술자가 커피를<br>진지하게 대하면
            </h2>
            <div class="space-y-4 text-gray-600 dark:text-slate-300 leading-relaxed">
              <p>개발자는 데이터를 믿습니다. 레퍼런스커피는 원두의 산지, 고도, 수확 시기, 가공 방식을 모두 기록하고 분석합니다.</p>
              <p>로스팅 온도, 시간, 냉각 속도 — 모든 변수를 정밀하게 제어합니다. 마치 프로덕션 서버를 모니터링하듯, 커피의 모든 순간을 관리합니다.</p>
              <p>그 결과물이 바로 레퍼런스커피입니다. 200명이 넘는 고객이 반복 구매로 신뢰를 보내주고 있습니다.</p>
            </div>
          </div>

          <!-- Stats Grid -->
          <div class="grid grid-cols-2 gap-4">
            {[
              { value: '200+', label: '누적 고객', color: 'text-coffee-700 dark:text-coffee-400' },
              { value: '4.8★', label: '평균 리뷰', color: 'text-coffee-700 dark:text-coffee-400' },
              { value: '7x', label: '성장률', color: 'text-coffee-700 dark:text-coffee-400' },
              { value: '재구매', label: '높은 재구매율', color: 'text-coffee-700 dark:text-coffee-400' },
            ].map(stat => (
              <div class="bg-coffee-50 dark:bg-slate-800 rounded-card p-6 text-center border border-coffee-100 dark:border-slate-700">
                <p class={`font-display font-black text-4xl ${stat.color} mb-2`}>{stat.value}</p>
                <p class="text-sm text-gray-600 dark:text-slate-400 font-display">{stat.label}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Products -->
    <section class="py-24 bg-gray-50 dark:bg-slate-800">
      <div class="max-w-8xl mx-auto px-6 lg:px-8">
        <div class="mb-12">
          <span class="font-display text-label text-terra tracking-widest uppercase block mb-3">Products</span>
          <h2 class="font-display font-bold text-4xl text-gray-900 dark:text-white">제품 라인업</h2>
        </div>
        <div class="grid md:grid-cols-3 gap-6">
          {[
            { icon: '🫘', title: '싱글 오리진', desc: '에티오피아, 콜롬비아, 케냐 등 엄선된 산지의 단일 품종 원두. 각 산지 고유의 맛을 느껴보세요.', tag: 'Single Origin' },
            { icon: '☕', title: '하우스 블렌드', desc: '레퍼런스커피 시그니처 블렌딩. 균형 잡힌 산미와 묵직한 바디감이 특징입니다.', tag: 'House Blend' },
            { icon: '🎁', title: '선물 세트', desc: '개발자 동료, 팀 선물로 인기. 3종 샘플러 패키지로 다양한 맛을 경험해 보세요.', tag: 'Gift Set' },
          ].map(p => (
            <div class="bg-white dark:bg-slate-900 rounded-card p-6 border border-gray-100 dark:border-slate-700 hover:shadow-card-hover transition-all duration-300">
              <div class="text-4xl mb-4">{p.icon}</div>
              <h3 class="font-display font-bold text-xl text-gray-900 dark:text-white mb-2">{p.title}</h3>
              <p class="text-gray-600 dark:text-slate-400 text-sm leading-relaxed mb-4">{p.desc}</p>
              <span class="font-mono text-xs text-terra">{p.tag}</span>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Why Data-Driven Coffee -->
    <section class="py-24 bg-slate-900 text-white">
      <div class="max-w-8xl mx-auto px-6 lg:px-8">
        <div class="mb-12">
          <span class="font-display text-label text-coffee-300 tracking-widest uppercase block mb-3">Why Different</span>
          <h2 class="font-display font-bold text-4xl">기술 스타트업의<br>커피가 다른 이유</h2>
        </div>
        <div class="grid md:grid-cols-3 gap-6">
          {[
            { icon: '📊', title: '데이터 기반 로스팅', desc: '온도·시간·냉각 모든 변수를 기록하고 분석. 매 배치마다 일관된 품질을 보장합니다.' },
            { icon: '🔬', title: '원두 트레이서빌리티', desc: '산지·고도·수확 시기·가공 방식을 모두 추적. 투명한 원두 이력관리.' },
            { icon: '🚀', title: '빠른 로스팅 & 배송', desc: '주문 후 신선하게 로스팅. 네이버 스마트스토어 통해 빠른 배송.' },
          ].map(item => (
            <div class="bg-slate-800 rounded-feature p-6 border-l-4 border-coffee-400">
              <div class="text-3xl mb-4">{item.icon}</div>
              <h4 class="font-display font-bold text-lg text-white mb-2">{item.title}</h4>
              <p class="text-slate-400 text-sm leading-relaxed">{item.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="py-20 bg-coffee-900 text-white">
      <div class="max-w-8xl mx-auto px-6 lg:px-8 text-center">
        <h2 class="font-display font-black text-4xl mb-4">지금 바로 주문하세요</h2>
        <p class="text-coffee-200 mb-8">네이버 스마트스토어에서 신선하게 로스팅된 원두를 만나보세요.</p>
        <a href="https://smartstore.naver.com/refcoffee" target="_blank" rel="noopener noreferrer"
          class="px-8 py-4 bg-coffee-400 text-white font-display font-bold rounded-btn hover:bg-coffee-600 transition-colors">
          스마트스토어 바로가기 →
        </a>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>
